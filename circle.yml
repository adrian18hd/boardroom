deployment:
  staging:
    branch: development
    commands:
      - cap acceptance deploy
  production:
    branch: master
    commands:
      - cap production1 deploy
      - cap production2 deploy
